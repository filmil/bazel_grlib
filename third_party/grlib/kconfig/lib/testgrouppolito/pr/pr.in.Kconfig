config LIB_TESTGROUPPOLITO_PR_PR_PARTIAL
    bool "Enable Partial Reconfiguration  "
#   if [ "$CONFIG_PARTIAL" = "y" ]; then
config LIB_TESTGROUPPOLITO_PR_PR_CRC
    bool "Enable Bitstream Verification (D2PRC-CRC mode)"
#         if [ "$CONFIG_CRC" = "y" ]; then
#           integer 'CRC block size ' CONFIG_BLOCK 100
#         fi
#         if [ "$CONFIG_CRC" = "n" ] ; then
config LIB_TESTGROUPPOLITO_PR_PR_EDAC
    bool "Enable Bitstream EDAC (D2PRC-SECDED mode)"
#           if [ "$CONFIG_EDAC" = "n" ] ; then
config LIB_TESTGROUPPOLITO_PR_PR_DCM_FIFO
    bool "Async Mode "
#           fi
#         fi
#         if [ "$CONFIG_DCM_FIFO" = "y" -o "$CONFIG_CRC" = "y" -o "$CONFIG_EDAC" = "y"]; then
#              choice 'FIFO depth' \
# 	      " 64		CONFIG_DPR_FIFO64 \
# 	       128		CONFIG_DPR_FIFO128 \
# 	       256		CONFIG_DPR_FIFO256 \
# 	       512 		CONFIG_DPR_FIFO512" 64
#         fi
#   fi
  
