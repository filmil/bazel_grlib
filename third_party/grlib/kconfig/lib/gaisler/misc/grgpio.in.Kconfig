
config GRGPIO_ENABLE
    bool "Enable generic GPIO port              "
if y # TODO: improve if translation
config GRGPIO_WIDTH
    int "GPIO width            "
    default 8
config GRGPIO_IMASK
    hex "GPIO interrupt mask   "
    default 0000
endif

