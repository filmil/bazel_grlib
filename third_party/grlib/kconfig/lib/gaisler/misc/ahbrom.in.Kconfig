config AHBROM_ENABLE
    bool "On-chip AHB ROM                   "
if y # TODO: improve if translation
config AHBROM_START
    hex "ROM start address (haddr[31:20]) "
    default 000
config AHBROM_PIPE
    bool "Pipelined ROM access              "
endif

