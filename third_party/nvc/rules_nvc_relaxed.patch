--- internal/vhdl_library.bzl
+++ internal/vhdl_library.bzl
@@ -73,6 +73,7 @@
             library_name,
           ),
           "-a",
+          "--relaxed",
         ] + [f.path for f in srcs],
         tools = [analyzer_x] + artifacts,
         # Only seems to work from bazel 6.0.0 on.
